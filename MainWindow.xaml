<Window x:Class="FindSuffixApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FindSuffixApp"
        mc:Ignorable="d"
        Title="Автодополнение слова" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.6*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="0" x:Name="AddTextBox" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextWrapping="Wrap"/>
        <Button Grid.Row="1" Content="Добавить текст" Width="150" Margin="4" VerticalAlignment="Top" Click="AddTextButton_Click"/>
        
        <TextBox Grid.Row="2" x:Name="PrefixTextBox" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextChanged="PrefixTextBox_TextChanged" TextWrapping="Wrap"/>
        
        <Label Grid.Row="3" x:Name="SuggestionLabel" Content="Подсказка: " VerticalAlignment="Top" HorizontalAlignment="Center"/>
    </Grid>
</Window>
